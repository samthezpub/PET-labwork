package org.example;

public class Math {
    public static double calculateA(double volume, int temp, double density, double mass, GasType gasType, ){

        double result = 0;
        return result;
    }
}
